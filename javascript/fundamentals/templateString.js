const name = "Bruno";

const template = `Olá, ${name}!`;
console.log(template);

// Expressions
console.log(`1 + 1 = ${1 + 1}`);

const up = (text) => text.toUpperCase();
console.log(`Ei... ${up("cuidado")}!`);
