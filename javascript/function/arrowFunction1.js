let double = function (a) {
  return 2 * a;
};

double = (a) => {
  return 2 * a;
};

double = (a) => 2 * a;
console.log(double(Math.PI));

let hello = function () {
  return "Olá...";
};

hello = () => "Olá";
console.log(hello());
